<?xml version="1.0" encoding="UTF-8"?>
<templates id="inher_history" inherit_id="point_of_sale.TicketButton" xml:space="preserve">

   
        <div class="ticket-button" t-att-class="{ highlight: props.isTicketScreenShown }" t-on-click="onClick">
            <div class="with-badge" t-att-badge="count">
                <i class="fa fa-ticket" aria-hidden="true"></i>
            </div>
            <div t-if="!env.isMobile">History</div>
        </div>
   
</templates>
